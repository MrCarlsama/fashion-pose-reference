# Local Corpus Directory

This directory is intentionally ignored by Git.

The open-source repository ships code, scripts, and the Codex skill only. Downloaded images, manifests, annotation batches, contact sheets, and generated outputs stay local unless you publish them separately with the proper rights.

Expected local layout:

```text
datasets/fashion_action_reference/
  manifest.jsonl
  images/
  pose_annotations/
```
